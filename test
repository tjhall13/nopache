#!/usr/bin/env nodejs

var nopache = require('./index.js');

var config = nopache.config({
    base: './html/',
    port: 2400,
    override: true,
    logfile: './temp/error.log'
});
nopache.cli(config, { });
